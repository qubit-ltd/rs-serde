[package]
name = "qubit-serde"
version = "0.4.0"
edition = "2024"
rust-version = "1.94"
authors = ["Haixing Hu <starfish.hu@gmail.com>"]
license = "Apache-2.0"
repository = "https://github.com/qubit-ltd/rs-serde"
description = "Reusable serde adapters and helper functions for Rust applications"
homepage = "https://github.com/qubit-ltd/rs-serde"
documentation = "https://docs.rs/qubit-serde"
readme = "README.md"
keywords = ["serde", "serialization", "utility", "qubit"]
categories = ["encoding", "development-tools", "rust-patterns"]
include = [
    "/Cargo.toml",
    "/LICENSE",
    "/README.md",
    "/README.zh_CN.md",
    "/RUST_CODING_STYLE.md",
    "/RUST_CODING_STYLE.zh_CN.md",
    "/doc/**/user_guide*.md",
    "/examples/**",
    "/resources/**",
    "/src/**",
    "/tests/**",
]

[lib]
name = "qubit_serde"
doctest = true
test = true

[dependencies]
qubit-datatype = { version = "0.8", path = "../rs-datatype", default-features = false, features = ["duration"] }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
postcard = { version = "1.1", default-features = false, features = ["use-std"] }
serde_json = "1.0"
